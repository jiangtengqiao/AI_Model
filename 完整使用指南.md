# 贤智AI - 完整使用指南

## 目录
1. [项目位置](#项目位置
2. [文件结构
3. [快速开始](#快速开始
4. [详细配置说明](#详细配置说明
5. [功能使用说明](#功能使用说明
6. [常见问题](#常见问题

---

## 项目位置

**完整项目位置：** `/workspace/`

**在这个目录下，您会看到以下文件和文件夹：

```
/workspace/
├── backend/           # 后端服务（Python + FastAPI
├── frontend/          # 前端应用（React + TypeScript）
├── crawler/         # 爬虫系统（Scrapy）
├── docs/            # 文档
├── scripts/         # 辅助脚本
├── setup.sh         # 快速设置脚本
└── README.md        # 项目说明
```

---

## 快速开始

### 方法一：使用一键设置脚本（推荐）

这是最简单的方式，只需一个命令完成所有设置：

```bash
cd /workspace
chmod +x setup.sh
./setup.sh
```

### 方法二：手动设置步骤

如果您想手动一步步设置，请按照以下步骤：

#### 第一步：设置后端

```bash
cd /workspace/backend

# 1. 创建虚拟环境
python -m venv venv

# 2. 激活虚拟环境
source venv/bin/activate
# Windows 系统使用：venv\Scripts\activate

# 3. 安装依赖
pip install --upgrade pip
pip install -r requirements.txt

# 4. 创建数据目录
mkdir -p data/chroma

# 5. 初始化数据库
python scripts/init_db.py

# 6. 添加示例数据
python scripts/add_sample_data.py
```

#### 第二步：配置环境变量

编辑 `/workspace/backend/.env 文件，填入您的配置：

```env
APP_NAME=贤智AI
DEBUG=True

# 数据库配置
DATABASE_URL=sqlite:///./data/xianzhi.db

# JWT密钥
SECRET_KEY=change-this-secret-key-to-something-secure-in-production-32-chars

# OpenAI API - 必须配置
OPENAI_API_KEY=sk-your-actual-openai-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
DEFAULT_MODEL=gpt-3.5-turbo

CHROMA_PERSIST_DIR=./data/chroma
```

#### 第三步：设置前端

```bash
cd /workspace/frontend

# 1. 安装依赖
npm install
```

#### 第四步：启动服务

**终端 1 - 启动后端：
```bash
cd /workspace/backend
source venv/bin/activate
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**终端 2 - 启动前端：
```bash
cd /workspace/frontend
npm run dev
```

#### 第五步：访问应用

打开浏览器，访问：**http://localhost:3000

---

## 详细配置说明

### 后端配置

#### 1. 数据库配置

- **SQLite（默认）**：无需额外设置，适合开发和演示
- **PostgreSQL**：生产环境推荐使用

#### 2. API 配置

必须配置 OpenAI API Key 才能使用 AI 问答功能。

获取 API Key：
1. 访问 https://platform.openai.com/api-keys
2. 登录您的 OpenAI 账户
3. 创建新的 API Key
4. 复制并填入 `.env` 文件

#### 3. JWT 密钥配置

用于用户认证的安全密钥，生产环境必须修改。

生成安全密钥的方法：
```bash
# 使用 Python 生成随机密钥
python -c "import secrets; print(secrets.token_urlsafe(32))
```

---

## 功能使用说明

### 1. 用户注册和登录

1. 打开 http://localhost:3000
2. 点击"注册"创建新账户
3. 使用邮箱和密码登录
4. 新用户默认获得一定的试用余额

### 2. AI 问答

1. 登录后进入聊天界面
2. 在输入框中输入您的问题
3. 点击"发送"或按回车
4. AI 将基于知识库回答您的问题

支持的问题类型：
- 学校介绍
- 办学理念
- 贤麟 IP 介绍
- 姊妹校信息
- 等等

### 3. 知识库管理

知识库数据存储在：
- 向量数据库：`/workspace/backend/data/chroma/
- SQLite 数据库：`/workspace/backend/data/xianzhi.db

添加更多数据：
编辑 `/workspace/backend/scripts/add_sample_data.py` 文件中的示例数据，然后运行该脚本

### 4. 爬虫使用

爬虫系统用于采集学校官网数据：

```bash
cd /workspace/crawler

# 安装爬虫依赖
pip install -r requirements.txt

# 编辑爬虫文件，添加目标网站
# 编辑 shangxian/spiders/school_spider.py

# 运行爬虫
scrapy crawl school
```

---

## 文件功能说明

### 主要后端文件

| 文件 | 位置 | 功能说明 |
|------|------|---------|
| `app/main.py | `/workspace/backend/app/main.py | FastAPI 应用入口，API 路由注册 |
| `app/api/auth.py | `/workspace/backend/app/api/auth.py` | 用户认证 API |
| `app/api/chat.py | `/workspace/backend/app/api/chat.py` | 聊天问答 API |
| `app/rag/chat_engine.py | `/workspace/backend/app/rag/chat_engine.py` | RAG 聊天引擎 |
| `app/rag/vector_store.py | `/workspace/backend/app/rag/vector_store.py` | 向量数据库管理 |
| `app/rag/document_processor.py | `/workspace/backend/app/rag/document_processor.py` | 文档处理模块 |
| `app/core/config.py | `/workspace/backend/app/core/config.py` | 配置管理 |
| `app/core/database.py | `/workspace/backend/app/core/database.py` | 数据库连接 |
| `app/core/security.py | `/workspace/backend/app/core/security.py` | 安全认证模块 |
| `app/models/user.py | `/workspace/backend/app/models/user.py` | 用户数据模型 |
| `app/models/order.py | `/workspace/backend/app/models/order.py` | 订单和交易模型 |
| `app/services/user_service.py | `/workspace/backend/app/services/user_service.py` | 用户服务逻辑 |

### 主要前端文件

| 文件 | 位置 | 功能说明 |
|------|------|---------|
| `src/main.tsx` | `/workspace/frontend/src/main.tsx` | React 应用入口 |
| `src/App.tsx` | `/workspace/frontend/src/App.tsx` | 主应用组件，路由配置 |
| `src/pages/Login.tsx` | `/workspace/frontend/src/pages/Login.tsx` | 登录注册页面 |
| `src/pages/Chat.tsx` | `/workspace/frontend/src/pages/Chat.tsx` | 聊天主界面 |

---

## API 文档

启动后端服务后，访问以下地址查看 API 文档：

- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

---

## 常见问题

### 1. 后端启动失败

**问题**：提示找不到模块
**解决**：确保已激活虚拟环境并安装了依赖

### 2. 前端启动失败

**问题**：npm install 失败
**解决**：检查 Node.js 版本（需要 18+）

### 3. AI 回答无响应

**问题**：发送消息后无回复
**解决**：检查 OpenAI API Key 配置是否正确

### 4. 数据库错误

**问题**：数据库连接失败
**解决**：检查 `.env` 中的数据库配置

---

## 开发调试

查看后端日志：
```bash
# 后端运行终端会显示详细日志
```

查看前端控制台：
```bash
# 浏览器按 F12 打开开发者工具
```

---

## 下一步

1. 配置 OpenAI API Key
2. 启动服务
3. 注册测试账户
4. 开始使用
5. 根据需要扩展知识库
6. 集成支付系统（高级功能）
```